apiVersion: v1
kind: Service
metadata:
    name: "sock"
    labels:
        app: "sock"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "sock"